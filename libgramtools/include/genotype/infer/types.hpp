namespace gram::infer {

    class AbstractGenotypedSite;

    struct Allele;
    using gt_site = AbstractGenotypedSite;
    using gt_sites = std::vector<AbstractGenotypedSite>;
    using allele_vector = std::vector<Allele>;
}
